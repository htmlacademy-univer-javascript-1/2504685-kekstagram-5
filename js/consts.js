const COMMENTS_STEP = 5;
const MAX_STRING_LENGTH = 140;
const ALERT_SHOW_TIME = 5000;
const MAX_SYMBOLS = 20;
const MAX_HASHTAGS = 5;
const DELAY = 500;
const COUNT_OF_FILTER = 10;
const FILE_TYPES = ['gif', 'jpg', 'jpeg', 'png'];

const Keys = {
  ESCAPE: 'Escape',
  ESC: 'Esc'
};

const Zoom = {
  MIN: 25,
  MAX: 100,
  STEP: 25,
};

export {MAX_SYMBOLS, MAX_HASHTAGS, Keys, COMMENTS_STEP, ALERT_SHOW_TIME, MAX_STRING_LENGTH, DELAY, FILE_TYPES, COUNT_OF_FILTER, Zoom};
